---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hello
  template:
    metadata:
      annotations:
        linkerd.io/inject: enabled
      labels:
        app: hello
    spec:
      containers:
      - name: hello
        image: cilium/echoserver
        ports:
        - containerPort: 8080
        env:
          - name: PORT
            value: "8080"
---
apiVersion: v1
kind: Service
metadata:
  name: hello
  labels:
      mirror.linkerd.io/exported: "true"
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: hello
---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: hello-lamda
spec:
  tls: {}
  entryPoints:
    - websecure
  routes:
  - match: HostRegexp(`hello-lamda.{domain:.+}`)
    kind: Rule
    services:
    - name: hello
      kind: Service
      port: 8080
---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: default
spec:
  tls: {}
  entryPoints:
    - websecure
  routes:
  - match: HostRegexp(`{any:.+}`)
    kind: Rule
    priority: 1
    services:
    - name: hello
      kind: Service
      port: 8080
