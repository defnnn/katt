apiVersion: v1
kind: Service
metadata:
  name: katt
  labels:
    app: katt
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: katt
  selector:
    app: katt
  type: ClusterIP
