apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - cloudflared.yaml

configMapGenerator:
- name: cloudflared-config
  files:
    - config

secretGenerator:
- name: cloudflared-secret
  files:
    - secret
